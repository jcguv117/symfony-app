{% extends 'base.html.twig' %}

{% block title %}Orders{% endblock %}

{% block body %}
    <h1>Orders</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idorder</th>
                <td>{{ order.idorder }}</td>
            </tr>
            <tr>
                <th>Idproduct</th>
                <td>{{ order.idproduct }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ order.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_orders_index') }}">back to list</a>

    <a href="{{ path('app_orders_edit', {'idorder': order.idorder}) }}">edit</a>

    {{ include('orders/_delete_form.html.twig') }}
{% endblock %}
