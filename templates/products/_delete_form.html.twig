<form method="post" action="{{ path('app_products_delete', {'idproduct': product.idproduct}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.idproduct) }}">
    <button class="btn">Delete</button>
</form>
